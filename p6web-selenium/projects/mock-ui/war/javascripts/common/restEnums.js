define(function () {
    "use strict";

    return {
        ID_PARAM: "id",

        LEVEL_PARAM: "level",

        OVERLAY_ID_PARAM: "overlayId",

        DataFormat: {
            // Synchronize with JacksonFormatterFactory.JSON_DATE_FORMAT
            DATE: "{MM}-{dd}-{yyyy} {HH}:{mm}:{ss}"
        }
    };
});